
#include <stdio.h>
#include <string.h>


int main()
{
    char str1[1001];
    char str2[1001];
    fgets(str1, sizeof(str1), stdin);  // read string
    fgets(str2, sizeof(str2), stdin);  // read string
    printf("str1: ");
    puts(str1);
    printf("str2: ");
    puts(str2);    // display string

    if(str == 'a' && str =='z')

    return 0;
}
