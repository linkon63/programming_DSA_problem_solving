#include <stdio.h>

int main(){
    // data type in c
    // int, float, char, double,
    //----- variable declaration
    // no space , you can use number , use underscore
    // no keyword use in c programming
    //  -----------------------
        // input output in c programming
      //  int a, b;
      //  scanf("%d%d", &a, &b);
       // printf("a:%d",a);
     //   printf("-- b:%d",b);

    // processing learning
    int a, b;
    scanf("%d%d", &a, &b);
    printf("a:%d",a);
    printf("-- b:%d",b);

    // sum of 2 number in processing not sum diff and multi

    // area of circle

return 0;
}
